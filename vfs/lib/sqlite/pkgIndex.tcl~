# -*- tcl -*-
# Tcl package index file, version 1.1
#
if {[package vsatisfies [package provide Tcl] 9.0-]} {
    package ifneeded sqlite3 3.47.2 \
	[list load [file join $dir $::tcl_platform(os) $::tcl_platform(machine) libtcl9sqlite3.47.2[info sharedlibextension]]]
} else {
    package ifneeded sqlite3 3.47.2 \
	[list load [file join $dir $::tcl_platform(os) $::tcl_platform(machine) libsqlite3.47.2[info sharedlibextension]]]
}
